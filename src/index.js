import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./app.js";

const root = ReactDOM.createRoot(document.getElementById("root"));
// Rendering means fetching HTML components and injecting them inside root element
root.render(<App />);
